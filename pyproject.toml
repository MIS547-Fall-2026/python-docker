[project]
name = "python-docker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tjgin", email = "gintiffany@arizona.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.115.12",
    "flask==3.1.3",
    "uvicorn==0.34.3",
]

[project.scripts]
python-docker = "python_docker:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
